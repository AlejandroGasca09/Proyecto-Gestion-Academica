/*
package com.hitss.springboot.proyectoGestionAcademica.Validation;

import com.hitss.springboot.proyectoGestionAcademica.services.UserService;
import jakarta.validation.ConstraintValidator;
import jakarta.validation.ConstraintValidatorContext;
import org.springframework.beans.factory.annotation.Autowired;

import java.lang.annotation.Annotation;

public class IsExistDbValidation implements ConstraintValidator<IsExistDb,String> {

    @Autowired
    private UserService service;

    /*
    @Override
    public boolean isValid(String s, ConstraintValidatorContext constraintValidatorContext) {
        return service(value);
    }

}
 */
